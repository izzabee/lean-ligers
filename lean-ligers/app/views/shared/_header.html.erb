<!DOCTYPE html>
<nav>
  <div class="login">
    <% if current_user %>
      <%= "Welcome, " + current_user.first_name  %>
    <% else %>
      Don't have an account yet? <button class="btn"><%= link_to 'Sign Up!', new_user_path %>
    <% end %>
    <%= button_to "Log Out", session_url, method: "delete" if current_user %>
  </div>
</nav>
